import { TransactionHashPipe } from './transaction_hash.pipe';

describe('TransactionHashPipe', () => {
  it('should be defined', () => {
    expect(new TransactionHashPipe()).toBeDefined();
  });
});
